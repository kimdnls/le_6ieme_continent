<!DOCTYPE html>
<html lang="fr">
<head>
  <title>Le 6ieme Continent</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- FontAwesome pour les icônes -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

  <!-- Bootstrap CSS 5.3.3 -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">

  <!-- Google Fonts -->
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;200;300;400;500;600;700&display=swap">

  <!-- Lightbox CSS -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.11.3/css/lightbox.min.css" rel="stylesheet">

  <!-- Rails CSRF et CSP Meta Tags -->
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Fichier CSS principal -->
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

  <!-- Fichier JS principal -->
  <%= javascript_tag 'application', 'data-turbolinks-track': 'reload' %>

  <style>
    body {
      padding-top: 180px; /* Ajustez la valeur pour laisser suffisamment d'espace pour les bannières */
    }

    .top-banner {
      z-index: 1001;
      height: 40px;
    }

    .banner_sticky {
      background-image: linear-gradient(180deg, rgba(0, 0, 0, .96) 0, rgba(0, 0, 0, .4) 100%);
      position: fixed;
      background-color: black;
      top: 20px;
      left: 0;
      width: 100%;
      z-index: 1000;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding-top: 20px;
    }

    .logo-container {
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .logo {
      max-width: 160px;
      height: auto;
    }

    .social-icons {
      color: #999;
      font-size: 1.2em;
    }

    .social-icons:hover {
      color: #EEEEEE;
    }

    .fa-facebook-f:before {
      margin-right: 20px;
    }

    .fa-instagram:before {
      margin-right: 10px;
    }

    .social-icons a {
      margin-left: 10px;
    }

    .content {
      padding-top: 10px;
    }

    /* Centrage de l'icône du menu */
    .navbar-toggler {
      margin: 0 auto;
      display: flex;
      justify-content: center;
      border-color: non;
    }

    /* Ajustement des rubriques dans le menu */
    .navbar-collapse {
      text-align: left;

    }

    .navbar-nav {
      display: flex;
      flex-direction: row;  /* Menu en ligne */
      align-items: center;
      width: auto;          /* Chaque rubrique prend sa propre taille */
    }

    .nav-link.invisible-button {
      font-weight: 200;
      font-family: 'Poppins', sans-serif;
      color: white !important;
      text-decoration: none;
      cursor: pointer;
      font-size: 14px;
      padding: 14px 0px;
    }

    .nav-link.invisible-button:hover {
      color: rgb(88, 88, 104) !important;
      transition-duration: 0.4s;
      transition-timing-function: ease-in-out;
    }

/* Pour les petits écrans (smartphones), menu en colonne */
@media (max-width: 767px) {
  .navbar-nav {
    flex-direction: column !important;  /* Affiche les éléments du menu verticalement */
    align-items: center;     /* Centre les rubriques */
    width: 100%;             /* S'assure que les rubriques occupent la largeur disponible */
  }

  .nav-item {
    margin-bottom: 10px;     /* Ajoute de l'espace entre les rubriques */
    width: 100%;             /* Chaque item occupe toute la largeur sur mobile */
  }

  .nav-link {
    text-align: center;       /* Centre le texte des rubriques */
    flex-direction: column;
    width: 100%;             /* S'assure que chaque lien occupe toute la largeur */
  }

  .top-banner {
    height: auto;
    padding: 5px 0;
  }

  .social-icons {
    font-size: 1.5em;
  }

  .banner_sticky {
    top: 0; /* Enlever l'espace pour un affichage correct sur mobile */
  }

  .logo {
    max-width: 120px; /* Réduction de la taille du logo pour mobile */
  }

  .navbar-toggler {
    margin: 0 auto;
  }

  .navbar-collapse {
    text-align: center;
  }
}

    /* Pour les écrans larges */
@media (min-width: 768px) {
  .top-banner {
    padding: 10px 0;
  }

  .logo-container {
    margin-bottom: 20px;
  }
}

  </style>
</head>

<body>

  <!-- Bannière blanche -->
  <div class="container-fluid bg-white border-bottom fixed-top top-banner">
    <div class="row">
      <div class="col text-right py-2">
        <a href="https://www.facebook.com/le6iemecontinent" target="_blank" class="social-icons">
          <i class="fab fa-facebook-f"></i>
        </a>
        <a href="https://www.instagram.com/le_6ieme_continent?igsh=dHJpNGdhd3B6aXdo" target="_blank" class="social-icons">
          <i class="fab fa-instagram"></i>
        </a>
      </div>
    </div>
  </div>

  <!-- Bannière noire avec logo centré -->
  <div class="banner_sticky">
    <div class="logo-container">
      <a href="<%= root_path %>">
        <%= image_tag 'logo.jpg', alt: 'Logo', class: 'logo' %>
      </a>
    </div>

    <nav class="navbar navbar-expand-lg navbar-dark bg-black w-100">
      <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to "Lunch", lunch_path, class: "nav-link invisible-button" %>
            </li>
            <li class="nav-item">
              <%= link_to "Événements", evenements_path, class: "nav-link invisible-button" %>
            </li>
            <li class="nav-item">
              <%= link_to "Plats Signatures", plats_path, class: "nav-link invisible-button" %>
            </li>
            <li class="nav-item">
              <%= link_to "Photos", photos_path, class: "nav-link invisible-button" %>
            </li>
            <li class="nav-item">
              <%= link_to "Contact", contact_path, class: "nav-link invisible-button" %>
            </li>
          </ul>
        </div>
      </div>
    </nav>
  </div>

  <!-- Contenu principal -->
  <div class="content">
    <%= yield %>
  </div>

  <!-- Bootstrap JS Bundle avec Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <!-- Lightbox JS -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.11.3/js/lightbox.min.js"></script>
</body>
</html>
