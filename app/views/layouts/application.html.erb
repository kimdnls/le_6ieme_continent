<!DOCTYPE html>
<html>
<head>
  <title>Le 6ieme Continent</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_tag 'application', 'data-turbolinks-track': 'reload' %>
  <style>
    .banner_sticky {
      background-image: linear-gradient(180deg, rgba(0, 0, 0, .96) 0, rgba(0, 0, 0, .4) 100%);
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      z-index: 1000;
    }

    .banner_sticky .navbar {
      background-color: black !important;
    }

    .logo {
      max-width: 200px;
      height: auto;
      display: inline-block;
    }

    .social-icons {
      color: #fff;
      font-size: 1.2em;
    }

    .social-icons a {
      margin-left: 10px;
    }

    /* Ajout d'un peu d'espace pour que la bannière blanche ne soit pas masquée */
    .content {
      padding-top: 100px; /* Ajustez en fonction de la hauteur de la bannière noire */
    }

    /* Décalage de la bannière noire */
    .banner_sticky.black {
      top: 40px; /* Hauteur de la bannière blanche */
    }
  </style>
</head>

<body>
  <!-- Bannière blanche -->
  <div class="container-fluid bg-white border-bottom fixed-top">
    <div class="row">
      <div class="col text-right py-2">
        <a href="https://www.facebook.com" target="_blank" class="social-icons">
          <i class="fab fa-facebook-f"></i>
        </a>
        <a href="https://www.instagram.com" target="_blank" class="social-icons">
          <i class="fab fa-instagram"></i>
        </a>
      </div>
    </div>
  </div>

  <!-- Bannière noire -->
  <div class="banner_sticky navbar navbar-dark bg-dark fixed-top black">
    <div class="container-fluid text-center">
      <div class="row w-100">
        <div class="col-12">
          <div class="logo-container text-center">
            <%= link_to root_path do %>
              <%= image_tag 'logo.jpg', alt: 'Logo', class: 'logo mx-auto d-block' %>
            <% end %>
          </div>
        </div>
        <div class="col-12">
          <div class="navbar-nav justify-content-center">
            <ul class="navbar-nav">
              <li class="nav-item"><%= link_to "Lunch", lunch_path, class: "nav-link invisible-button" %></li>
              <li class="nav-item"><%= link_to "Événements", evenements_path, class: "nav-link invisible-button" %></li>
              <li class="nav-item"><%= link_to "Plats Signatures", plats_path, class: "nav-link invisible-button" %></li>
              <li class="nav-item"><%= link_to "Photos", photos_path, class: "nav-link invisible-button" %></li>
              <li class="nav-item"><%= link_to "Contact", contact_path, class: "nav-link invisible-button" %></li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Contenu principal -->
  <div class="content">
    <%= yield %>
  </div>

  <%= javascript_include_tag 'application' %>
</body>
</html>
